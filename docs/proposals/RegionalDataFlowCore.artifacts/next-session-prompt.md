# Next Session: post-K1 (owner ranks: Tier-2 provenance vs fuzz arm vs Stage-C re-brief vs the K6 riders; K3's two STOPs standing)

Continue in the Dr. Lojekyll repository, branch `keyed-instances` (tip =
the session-7 close docs commit atop `e641be46` [K1], PUSHED; clean tree
expected). Governing discipline unchanged: reality grounding, byte-exact
goldens, explicit negative witnesses, predict-then-verify at the
single-transform grain, the evil-monkey rule, complexity guilty until
proven inherent (smaller-not-larger).

THE ENTRY SEEDS: `key-pragma-landed-seed.md` (session-6) remains the
whole-program pipeline view, now read WITH `k1-multikey.md` (session-7, the
K1 working authority) — k1-multikey.md Part A supersedes the seed's fine
grain wherever they overlap (@key parse states, Step 2b, the admissibility
fork, the witness stack), and its PANEL RECORD + Part C hold the landed
adjudications ADJ-K1-A..J and golden bytes. Four session-6-seed drifts are
recorded in k1-multikey.md's header (Connect.cpp location; the ControlFlow-
Build fence placement; 11-not-12 symlinks; the demand__-collision advice
fork — the last is an OPEN rider). Both seeds are orchestrator-authored —
fleet-verify anchors before building. The normative design record is
`region-model-diffs.md` (RP-1..RP-10, RES-1..6, the SESSION-7
RATIFICATIONS).

THE STANDING METHOD is the session's shape — for whichever slice the owner
ranks first:
(a) BUILD OUT THE PSEUDOCODE: fleet re-verify + extend against the tip;
    ONE pseudocode authority per slice (k1-multikey.md is the K1
    precedent-shape: Part A grounded, Part B diffs, panel record, Part C
    bytes).
(b) FORMULATE THE DIFFS: rigorous dated amendments on the verified
    pseudocode (K3: stage-c-diff.md AMENDED by the DIFF-R1 panel's 15
    normative amendments — no implementation before dated diffs; K5:
    DIFF-NEXT-3 Tier 2 + D2.9 as ONE slice). Opus authors; TIGHTEN THE
    AUTHORING PASS first (orchestrator adjudicates open sub-questions
    into the prompt — the session-7 ADJ-K1 pattern).
(c) CRITIQUE THE DIFFS: adversarial panel (correctness/lifecycle,
    termination/confluence, testability/oracle, necessity),
    REFUTE-VERIFIED against code — no finding without a failure scenario;
    orphaned findings (a dead refuter) are ORCHESTRATOR-verdicted from
    the journal, never dropped (session-7 precedent: the bless_copy
    finding survived its refuter's death and landed as ADJ-K1-G).
    CALIBRATION: the panel is a FILTER; the SUITE is the referee.
(d) DESIRED IR OUTPUT STATES: byte-exact, computed from the EMITTERS'
    padding rules over FRESHLY COLLECTED dumps; front-load the dump
    collection while panels run (they are cheap and become the G-gate
    predictions). Implementation gates on predict-then-verify: dumps must
    byte-match Part-C predictions BEFORE goldens install (finding, never
    fudge).
Implementation only when (a)–(d) are green and the owner ratifies.

RUN FOLLOW-ON WORK THROUGH THE WORKFLOW TOOL — deterministic orchestration
(pipeline / fan-out / adversarial-verify), a thin orchestrator, structured
findings. MODEL TIERING: sonnet for mechanical extraction; opus for
judgment. SILENT tests. NEVER bless a golden to make anything pass; the
`bless_copy` guard now MECHANIZES the symlink rule (a symlink golden is
never written through — skip on byte-identity, BLESS-REFUSED on
divergence), but the review-before-bless discipline stands. Any
demand-blind consumer of `Query::Build` MUST pass `suppress_demand=true`
(S6-IMPL-1; bin/Oracle is the precedent). Do not commit/push unless asked;
identity `Peter Goodman <peter.goodman@gmail.com>`; keep tool/model/
provider details out of commits and artifacts.

## State at session open (session-7 close — K1 LANDED)

- Multi-`@key` repetition (RP-10, K1): N `@key(...)` pragmas per
  `#local`/`#export` decl, TOTAL BIJECTION against the SIP-inferred
  adornment sets (order-free set-of-sets; Arm A declared-surplus / Arm B
  inferred-surplus named-set rejects); parse-time order-free duplicate-SET
  reject (ADJ-K1-A); per-set `declared-key` contract lines (written-pragma
  order, always-on no-match belt); the subgraphs[0] shared-demanded_decl
  assert. Storage `instance_key_param_index_sets`, accessor
  `InstanceKeys()` (the single-set `InstanceKey()` is GONE).
- The RP-8 auto-sweep STOP is RESOLVED: STRICT stays, for now. Best-effort
  re-opens only on an explicit owner call (the demand_*_body_1 oracle pins
  still anticipate it).
- Suite PASS (249 = 203 golden cases + 46 driverless rejects, both mode
  extremes); ctest 7/7. Witness spine: key_tc_witness (flat fallback,
  11 symlinks), key_neighborhood_witness (flagless nested,
  kSubgraphInstantiate=1), key_multi_adorn_witness (flagless nested N=2,
  kSubgraphInstantiate=2, stdout symlink + own rel.opt + contract.opt —
  the contract golden byte-locks the declared-key pairing), the TEN
  flagless key_* diagnostics, reject_key_double_1 (parse dup-set,
  WITH a bound query so a dropped check is a LOST CHECK).
- The injected forcing seam STILL EXISTS in both lowerings — deleting it
  is Stage C's request-edge work, the owner's stated end-state.

## Phase 0 (housekeeping)

1. `git status` / `git log -5` — expect clean at the session-7 close tip.
   Rebuild debug; suite SILENT (expect `SUITE: PASS (249)`); ctest 7/7.
2. Read, in order: this prompt, `k1-multikey.md` (skim Parts A + panel
   record), the ranked slice's sections in `region-model-diffs.md`, and
   for K3 additionally `stage-c-diff.md` + the DIFF-R1 panel record +
   `owner-adjudication-record.md`.

## Owner decisions to surface (STOP where load-bearing, do not guess)

- THE RANKING over the remaining path (K1 done; K2 resolved):
  1. **K5 Tier-2 provenance** — origin decl-sets on models (Connect
     erasure site, union-only on CSE folds) + D2.9 proxy-role inheritance
     as ONE slice; unblocks the ADJ-R3-C column-survival belt + undemanded
     interior naming; the panel must litigate the post-F1
     maintained-satellite question explicitly.
  2. **K4 the key-subset covering-array fuzz arm** — placements over
     (relation × key-subset × redeclaration); accept-iff-exact-match +
     clean-reject-otherwise + never abort; feeds rejects/. Small,
     independent.
  3. **K3 Stage-C re-brief** — request edges + lifecycle ops; kills the
     injected forcing seam. DESIGN-ONLY until the D2.6 reader-handle
     schema + §6-vs-§11 routing STOPs are answered; fold the DIFF-R1 15
     amendments into stage-c-diff.md first.
  4. **K6 riders** (any ranking; small): the demand__-collision rejects'
     advice fork on pragma_activated (found session 7); cross-
     redeclaration @key consistency; pragma-anchored Step-2b error ranges
     (store the pragma DisplayRange); R-K1-BATCHES (a definitional
     referee — .batches family — for the multi-adornment witnesses); the
     "demand key"/"instance key" wording normalization (F-1); the DOT
     `declared` badge + DR-IR DOT twin; the `KEY (_MissingVar)` wart.

## Stop conditions

An unratified decision becomes load-bearing (K3 carries the two explicit
owner STOPs); a predicted dump diverges unexpectedly; ANY golden beyond
the predicted set diverges; behavioral-golden divergence is a HARD STOP
(frozen through the Stage-C cutover); any impulse to author code for an
unranked slice. An unexpected corpus abort/divergence is a FINDING to
adjudicate and record — amend the design record, never fudge.

## Deliverables

The ranked slice through (a)–(d) with its one-authority artifact
(k1-multikey.md is the shape); if the owner ratifies implementation, the
slice landed with every golden byte-identical except the predicted set;
an updated version of THIS prompt pointing at the ranked-next slice.
