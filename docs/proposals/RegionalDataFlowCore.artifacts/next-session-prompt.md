# Next Session: post-K5 (owner ranks: K4 fuzz arm vs K3 Stage-C ownership flip vs S4 residuals; K3's two STOPs standing)

Continue in the Dr. Lojekyll repository, branch keyed-instances (tip = the
session-8 close docs commit atop `81385a92` [K5], pushed; clean tree
expected). Governing discipline unchanged: reality grounding, byte-exact
goldens, explicit negative witnesses, predict-then-verify at the
single-transform grain, the evil-monkey rule, complexity guilty until
proven inherent, Tigerstyle assertion density (the session-8 standing
directive).

Entry seed: docs/proposals/RegionalDataFlowCore.artifacts/k5-landed-seed.md
— Part 1 the pipeline DELTA on k6-landed-seed.md Part 1 (which remains the
whole-program pseudocode), Part 2 the re-ranked path (S2/S3/S4 — S1/K5 is
LANDED and RETIRED), Part 3 verify-first. The seed is single-pass
orchestrator-authored: fleet-verify its anchors before building.
Fine-grain authorities: k5-provenance.md (Parts A-E + the 17-finding panel
record; Part A's replacement-site census and Part E's padding-computed
byte-diff method are reusable instruments), k1-multikey.md, k6-riders.md;
the normative design record is region-model-diffs.md (RP-1..RP-10, THE
SUBGRAPH-AUTHORITY FRAMING — the input to any K3 work).

The standing method for whichever slice the owner ranks first: (a) build
out the pseudocode — fleet re-verify + extend against the tip, one
authority; (b) formulate the slice as rigorous dated diffs on that
pseudocode (opus authors; orchestrator adjudicates open sub-questions into
the prompt first and runs cheap empirical probes beside the panels — the
K5-Q7/K5-Q1 probes and the F31 probe are the exemplars); (c) critique the
diffs — adversarial panel across correctness/lifecycle, termination/
confluence, testability/oracle, necessity, refute-verified against code,
no finding without a failure scenario, orphaned findings
orchestrator-verdicted; negative witnesses must be able to LOSE the check
they pin (and state their MODE honestly — the K5 union referee is
opt/nocf-only, K5P-oracle-1); (d) author desired IR output states as byte
diffs from freshly collected dumps, computed from the emitters' padding
rules (E-K5-PAD: the region member-key column is a per-dump MAX — never
assume byte-additivity). Implementation only when (a)-(d) are green and
the owner ratifies, gated predict-then-verify, with an independent
orchestrator referee pass after (the K5 referee pass caught the
is_sorted-tolerates-adjacent-equals assert gap — asserts get refereed too).

Run follow-on work through the Workflow tool — deterministic pipeline/
fan-out/adversarial-verify orchestration, thin orchestrator, structured
findings. Model tiering: sonnet for mechanical extraction, opus for
judgment. Silent tests. Never bless to make anything pass (bless_copy
refuses symlink write-through mechanically; NOTE the bless-verbosity wart —
"blessed" lines on identical non-symlink bytes are no-ops, verify content
deltas with git). Any demand-blind Query::Build consumer passes
suppress_demand=true.

Owner decisions to surface: the ranking over S2 (K4 key-subset fuzz arm —
its oracle must model the K6-widened reject surface), S3 (K3 the Stage-C
ownership flip — DESIGN-ONLY until the D2.6 STOP [readers hand onto the
REGION or the STORE?] and the §6-vs-§11 routing rule are answered, plus the
DIFF-R1 15-amendment fold; K5's origin sets are now available input for
Stage-C member naming), and S4 residuals (the request-port declared badge;
the CONFIRMED behavioral .drflags comment-vs-code contradiction — 9 demand
cases build behavioral demand-ON, reconcile and re-adjudicate what those
goldens pin; the missing oracle-vs-behavioral cross-family check; the K5-D8
riders: the has_one_insert dead branch, the Merge-vs-Join CDaGI guard-tuple
inconsistency, D2.9(β); the bless-verbosity wart). Phase 0 expects
SUITE: PASS (250) and ctest 7/7.
