#message edge_2(u64 From, u64 To).
#message probe(u64 From, u64 To).
#local path(u64 From, u64 To).
path(F, T) : edge_2(F, T).
path(F, T) : path(F, M), edge_2(M, T).
#query reach(bound u64 From, free u64 To) : path(From, To).
#message missing(u64 From, u64 To) : probe(From, To), !path(From, To).
