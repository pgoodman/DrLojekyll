# Optimization differential-testing findings

## Divergence bugs (mode-dependent behavior)

### F1 [FIXED]: recursion.dr — assertion failure ONLY with dataflow opt enabled
- Repro (verified resolved 2026-07-07: exit 0): `drlojekyll data/examples/recursion.dr -ir-out /dev/null`
- Was: `Assertion failed: (view != user)` in QueryImpl::RelabelGroupIDs (lib/DataFlow/Optimize.cpp) — recursion.dr's self-only unions (`direct_only(A) : direct_only(A).`) hit QueryMergeImpl::Canonicalize's single-operand fast path (lib/DataFlow/Merge.cpp), which called ReplaceAllUsesWith even when the replacement was the union itself (its sole source, directly or through a perfectly-forwarding tuple), putting the view into its own columns' user lists. Only triggered with dataflow opt on because Canonicalize only runs then.
- Resolved by the F7 fix: the fast path now marks self-only unions unsatisfiable instead of self-replacing. OptDiff case `recursion` passes all 4 modes byte-identically.

### F2 [FIXED]: disappearing_invalid.dr — SIGSEGV ONLY with dataflow opt disabled
- Repro (verified resolved 2026-07-07: exit 0 in nodf and opt): `drlojekyll data/examples/disappearing_invalid.dr -disable-dataflow-opt -ir-out /dev/null`
- Was: EXC_BAD_ACCESS deref of null `UseRef<DataIndexImpl>` at Util/DefUse.h:742 during control-flow build/codegen when the unoptimized dataflow IR kept an infeasible join/comparison (impossible(1,B) :- input(B), one(B), B=2 with one(1) the only fact) — the *combination* nodf+cf-opt crashed.
- Most plausible resolver is the F5 fix (TUPLECMP zero-pair strict compare folds to false instead of assert(false)), which is exactly the nodf+cf-opt infeasible-comparison shape; the Database.cpp codegen rework may also have removed the null-index deref site. Exact attribution not bisected.

## Pre-existing all-mode failures (branch feature gaps / corpus rot) — excluded from corpus
- KV indices unimplemented in CF build: average_weight, pairwise_average_weight ("TODO(pag): KV Indices", Build.cpp:2211)
- Cross-products in removal regions unimplemented: conditions_to_bools ("TODO: Cross-products!", Build.cpp:2148)
- Parser-rotted examples (old syntax: `bytes` type, `#prologue`, etc.) are deleted from data/, along with the unreferenced data/invalid_syntax_examples, data/invalid_codegen_examples, and data/self_testing_examples/{cxx,python} corpora — data/ now holds 36 files: 33 compile in all 4 modes, 3 are the feature-gap files above.

## Corpus status (4-mode compile matrix over data/examples + data/self_testing_examples)
- 33 files compile in all 4 modes; dataflow opt changes IR in ~28, CF opt in all.
- Harness: tests/OptDiff/diffrun.sh (modes: opt, nodf, nocf, none; byte-compare stdout vs opt)
- Seed case validated end-to-end: tests/OptDiff/cases/transitive_closure.{dr,main.cpp} — all modes agree.

## Round 2 (differential fleet, 128 cases: 100 ok, 4 diverge, 13 crash, 11 infeasible)
Fixed so far:
- F4 std::tie(literal) in ordered-compare codegen -> std::make_tuple (Database.cpp) [FIXED]
- F5 TUPLECMP assert(false) on zero-pair strict compare (CF Optimize.cpp:764) -> legal fold to false [FIXED]
Fixed by agents:
- F6 [FIXED] Union.cpp:24 assert was stale: const-ref merge cols from compare-sinking are sound (all arms filter on same constant); assert now checks the true invariant (all preds resolve to same constant column)
- F7 [FIXED] single-operand MERGE fast path now marks self-only unions unsatisfiable (mirrors general path); convergence hash skips views that unlink themselves mid-canonicalization
- F8 [FIXED] two-layer soundness bug: (1) positive CONDitions were tested with OR semantics (single TUPLECMP over all counters) instead of AND -> now one TUPLECMP per condition chained; top-down wrap no longer discards the negative test when both exist; (2) per-equivalence-set constant map let X=2 clobber X=1 -> GuardViewWithFilter now guards each assignment with its own constant column; AllConstantsView col_index bug + promote-before-guard also fixed
- F10 [FIXED] ParsedLiteral::Spelling(lang) only searched info[lang].constants, but untagged #constant definitions are linked into info[kUnknown] -> nullopt -> codegen emitted int{} for every use; Spelling now searches the requested language's list then falls back to the kUnknown list (Parse.cpp); @unique folding in Compare.cpp was correct all along — divergence was downstream of the lost literal (cf15_3, compare_5)
Fixed in the final session:
- F9 [FIXED] zero-arity clause head + bare body predicate: GuardWithTuple(force) in BuildClause substituted the raw SELECT out of io->receives/rel->selects, so ConnectInsertsToSelects hit a TUPLE -> SIGSEGV (message) / ProxySelects assert Connect.cpp:128 (#local), all modes; cond guard is now a plain forwarding tuple (Build.cpp). Fixing the crash unmasked five more bugs in the dead-condition path, all fixed: (1) PrepareToDelete treated a dead last setter's condition as always-true (DropSetConditions cleanup stripped tests) -> new DropSetConditionsOfDeadView kills positive testers and drops negative tests (View.cpp); (2) IsConditional called unsat/dead views and empty MERGEs "always has data", so ShrinkConditions dropped never-satisfiable conditions as trivially true (View.cpp); (3) EliminateDeadFlows' sweep list was never populated — the whole mark-and-sweep was a no-op, so underivable cycles were never reclaimed; sweep now runs, unsat views are never tainted, dead members are pruned from surviving merges (DeadFlowElimination.cpp); (4) deleting a view now cascades forward (non-MERGE column readers die, MERGEs lose the source and die when it was the last), and a query whose derivations are all proven empty keeps its entry point via an always-empty table (ControlFlow Build.cpp); (5) the working sweep exposed a mode-dependent "Last receive of message is unused" ERROR when optimization kills all consumers of a message (deadflowelimination_2/5) — the last RECEIVE of a message now stays as the message's external interface, and ExtractPrimaryProcedure accepts its unused vector (Select.cpp, ControlFlow Procedure.cpp). Verified: optimize_5, deadflowelimination_1-6 + 44 other targeted cases pass all 4 modes byte-identically; ctest 3/3; data/ corpus compiles
- F11 [FIXED] two wrong-deletion defects in differential maintenance (all modes identically wrong, so diffrun never caught them — both new regression drivers self-check against an oracle and exit nonzero): (a) union sibling-branch support was never persisted — FillDataModel only materialized deletion-capable predecessors inside inductions, so a non-inductive union's branch checkers bottomed out at the RECEIVE's return-false and `rem a(1)` deleted a tuple still supported by `b(1)`; FillDataModel now materializes every predecessor of a deletion-receiving view (Build.cpp). (b) insert-side joins skip rows a same-batch removal cascade has transiently flipped kUnknown, and the missed derivations were never replayed (non-linear TC: add (2,3) + rem (1,1) over {3->1,1->1,4->3} permanently lost tc(2,1); 18/30 random-oracle seeds failed); rows may only be re-checked after the cascade quiesces, so the induction output region now snapshots still-unknown rows into a $induction_recheck vector before any checker call, re-proves them after the output pass, re-seeds proven rows into the induction input vectors, and codegen re-enters the fixpoint until the input vectors stay empty (Induction.cpp BuildUnknownRecheck, NeedsInductionOutputVector now forced for differential inductive merges, Database.cpp EmitInduction outer loop; the Join.cpp present-only guard stays — calling checkers mid-cascade is unsound). The "over-retains" half of the original note never reproduced; it is the failure mode of the naive fix (checker in the join guard). New cases union_sibling_diff + tc_nonlinear_diff (deterministic hazard + 12-seed oracle stress); both fail against the pre-fix compiler and pass all 4 modes; 30-seed external stress 0/30 mismatches in all 4 modes; transitive_closure{,2,3,4,5,_diff,_diff2,_lazy,_multiple_clause_bodies}, booleans{,_diff}, cf16_1/2/3/4/6, select_6, product_ind, recursion, two_inductions, negate_1, merge_1/2/3/6, kcfa_tiny{,_merged}, fibonacci, force, deadflowelimination_1 all pass 4 modes byte-identically; ctest 3/3
- F12 [FIXED] @product inside recursive cycle dropped derivations depending on message arrival order (all modes): for a PRODUCT straddling an inductive back-edge, ContinueProductWorkItem::Run minted a fresh never-appended TABLEPRODUCT input vector for each inductive predecessor instead of the induction's swap vector that BuildInductiveSwaps fills each fixpoint round, so (new inductive-side tuple) x (older other-side rows) pairs were never derived; Run now takes unseeded vectors from induction->view_to_swap_vec (Product.cpp). New order-varying regression case product_ind (driver runs 3 message orders, exits nonzero if results differ); product_ind, kcfa_tiny, kcfa_tiny_merged, merge_6, optimize_2/3, view_6, transitive_closure all pass 4 modes byte-identically; ctest 3/3
- F13 [FIXED] fibonacci.dr diverged by design (unbounded N -> infinite least model, and fib(47) overflows i32 since add_i32 wraps); case now bounds the recursion with N < 47 (fib(46) is the largest i32 Fibonacci number), driver dumps 0..47 incl. one past-bound empty scan — all 4 modes pass byte-identically (data/examples copy synced)
- F14 [FIXED] aggregates/KVINDEX TODO asserts instead of clean diagnostics: Program::Build now takes an ErrorLog and runs a feature-gap pre-pass rejecting surviving AGG / KVINDEX / impure-MAP / 0-pivot-differential-JOIN views with proper diagnostics (Program.h, ControlFlow Build.cpp, Main.cpp), dominating every TODO assert (kept as internal backstops); aggregate_1 and kvindex_2/3/4 now exit 1 with a rendered error in all 4 modes, and the 3 feature-gap data/examples get the same diagnostics. kvindex_1 now splits by mode BY DESIGN (KVINDEX->TUPLE elimination is a dataflow opt: compiles under opt/nocf — byte-identical output — clean error under nodf/none), so it is no longer a 4-mode diffrun case. The "unsat KVINDEX not removed" half was a misdiagnosis: kvindex_3's CMP(1==2) is never marked unsat because IsUniqueConstant() holds only for tags/unique foreign constants, never plain literals (literal evaluation out of scope); the unsat/dead-flow sweep itself has been active since the F9 fix

## Round 3 (unit-conditions Stage 0 pinning, 2026-07-07)

### F15 [FIXED]: condition flip fails to rip back downstream rows when the gated data path is deletion-capable
- Found while pinning `cond_diff_flipflop`. Minimal divergent repro (min5): `#message support(i32 S) @differential. #message item(i32 X) @differential. #local backing(i32 S). #local stored(i32 X). #query gated(free i32 X). backing(S):support(S). stored(X):item(X). c:backing(_). gated(X):stored(X),c.` Driver: add item 1; add support 10 (gated: 1 in all modes); remove support 10 — gated must retract, but opt/nocf kept the stale row while nodf/none retracted (divergence keyed solely on -disable-dataflow-opt). Trigger: the tested data path must be deletion-capable (`item @differential`); with an add-only `item` all 4 modes agree. Both polarities symmetric: with a `!c` clause, opt also failed to retract `ungated` rows when the gate opened.
- Root cause, one site: `EvaluateConditionAndNotify` (lib/ControlFlow/Build/Build.cpp). The counter-flip rip-back region scans each user's table, and when the user `CanReceiveDeletions()` it gated each row's removal on the user's full top-down checker. That checker re-proves the row under current semantics — including the condition that just flipped against it — so it rejects every row and the removal arm never runs. Whether the checker tests the condition depends on dataflow opt: under opt the condition-guard TUPLE merges into the data view and the scanned view's checker calls the `^test:` proc (self-defeating); under -disable-dataflow-opt the scanned view keeps a data-only checker (retraction works), hence the mode split.
- All-modes-wrong variant (diffrun-invisible, F11-style; min6): with the condition and gated clause reading `@differential` messages directly (`c : support(_). gated(X) : item(X), c.`), every mode kept the stale row — the scanned view's checker there also bottoms out self-defeatingly. Verified wrong pre-fix in opt and none; fixed by the same change.
- Fix: the rip-back direction no longer calls the top-down checker at all — no correct row gate exists there, since the view's checker tests the very condition that just flipped. Removal regions now run for every scanned row and the per-successor state transitions (present -> unknown, then top-down recheck) do the filtering, which is exactly how the already-correct add-only shape worked (no checker gate when `!CanReceiveDeletions()`). The push-through (addition) direction keeps the checker: the condition now passes, so it degenerates to the intended data-validity re-proof.
- Verified: min5 and min6 pass all 4 modes with correct retraction; `cond_diff_flipflop` carries the full shape (`item @differential`; item retraction while gate open, while closed, and in a same-batch swap) and passes all 4 modes; targeted sweep cond_multi_setter, cond_in_induction{,_deep}, cond_both_polarities, cond_double_test, booleans{,_diff}, negate_3, optimize_5, deadflowelimination_4/5 all 4-mode green; ctest 3/3; full suite SUITE: PASS.

## Final suite state (122 driver-equipped cases x 4 modes; regression cases added by fixes, incl. product_ind for F12 and union_sibling_diff + tc_nonlinear_diff for F11)
- All findings F1-F14 are [FIXED] (F13 was a by-design infinite least model, resolved by bounding the case).
- Case inventory: 117 four-mode diffrun cases + 4 clean-error-all-modes cases (aggregate_1, kvindex_2/3/4 = expected-unsupported diagnostics since the F14 fix) + kvindex_1, which compiles only when dataflow opt is on (opt/nocf run byte-identically — re-verified; nodf/none exit 1 with a rendered diagnostic — see F14).
- End-of-session regression sweep (2026-07-07): all 43 ledger-named 4-mode cases (booleans{,_diff}, cf15_3, cf16_1/2/3/4/6, compare_5, deadflowelimination_1-6, fibonacci, force, kcfa_tiny{,_merged}, merge_1/2/3/6, negate_1, optimize_2/3/5, product_ind, recursion, select_6, tc_nonlinear_diff, transitive_closure{,2,3,4,5,_diff,_diff2,_lazy,_multiple_clause_bodies}, two_inductions, union_sibling_diff, view_6) plus 12 prefix-spread samples (cf13_2, cf14_5, compare_1, conflicting_constants, disassemble, elim-cycle-simple, evm_func_parse, insert_3, join_4, map_2, tower_of_compares, tuple_4) — 55/55 pass all 4 modes byte-identically, TIMEOUT=120. aggregate_1 and kvindex_2/3/4 exit 1 with a rendered error in all 4 modes (no asserts). The remaining 62 four-mode cases were last verified in the per-fix targeted sweeps above.
- Historical crash repros re-verified resolved: F1 (recursion.dr, opt) and F2 (disappearing_invalid.dr, nodf) both exit 0.
- ctest 3/3 green (MiniDisassembler, PointsTo, Runtime); corpus sweep failures = exactly the 3 feature-gap examples (aggregates/KV indices, cross-products), which now fail with clean F14 diagnostics instead of asserts

## Round 4 (unit-conditions Stage D, 2026-07-07)
- No new findings. Open-bug list: empty (F1-F15 all [FIXED]).
- Case inventory: 128 cases — 123 four-mode diffrun/golden cases (incl. the 6 Stage 0 condition cases cond_multi_setter, cond_in_induction{,_deep}, cond_both_polarities, cond_diff_flipflop, cond_double_test) + the 5 expected-diagnostic cases encoded in runall.sh (aggregate_1, kvindex_1-4).
- data/examples/conditions_to_bools.dr stays a feature-gap file in all 4 modes: its final clause is an explicit `@product` join of two deletable locals, so it hits the differential cross-product diagnostic ("Cross-products over differential (deletable) data are not yet supported", the F14 pre-pass). Condition tests themselves compile as one-pivot unit-relation joins and never create products; only the user-written product keeps this file red.
