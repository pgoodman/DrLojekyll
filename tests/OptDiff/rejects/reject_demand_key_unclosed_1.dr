; An UNCLOSED @demand argument list hits the terminating period -- "Expected
; ')' to close the demand key" -- never a hang or run-off-the-buffer.
#message b_1(u64 X).
#local foo(u64 A) @demand(A.
foo(A) : b_1(A).
