; A SECOND @key pragma on one declaration -- the RESERVED multi-adornment
; lift path (`@key(A) @key(B)` -> N keyed stores) draws a clean
; not-yet-supported reject today.
#message b_1(u64 X, u64 Y).
#local foo(u64 A, u64 B) @key(A) @key(B).
foo(A, B) : b_1(A, B).
